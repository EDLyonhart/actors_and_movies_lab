<h1><%= @actor.name %></h1>

<div>
  <h2>Movies</h2>
  a

  </ul>
  <%= form_tag add_movie_path do |f|%>
  <select name="movie_id">                    <!-- dropdown menue. https://developer.mozilla.org/en-US/docs/Web/HTML/Element/select -->
    <% @moviesremain.each do |flick| %>
    <option value="<%=flick.id%>"><%=flick.title %></option>
    <% end %>
  </select>
  <input type="submit" value="Add">
  <% end %>
</div>

<br><br>
<%= link_to "Edit", edit_actor_path(@actor) %>
<%= button_to "Delete", actor_path(@actor), :method => :delete %>

<br><br>

<%= link_to "Back to Actors", actors_path %>